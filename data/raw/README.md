# Raw Data Directory

This directory is reserved for **raw MCU datasheets** used during local data extraction experiments.

## Notes
- Raw datasheet files (PDF / HTML) are **NOT tracked** in this repository.
- All raw data files are excluded via `.gitignore`.
- This repository only documents **what data is expected**, not the raw files themselves.

## Expected Structure
